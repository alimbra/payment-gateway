# config/packages/nelmio_api_doc.yaml
nelmio_api_doc:
  areas:
    path_patterns: # an array of regexps (document only routes under /api, except /api/doc)
      - ^/api(?!/doc$)
  documentation:
    info:
      title: Payment API
      description: This is an awesome api that can handle basic payment processing, including authorization, capture, and refund functionalities!
      version: 1.0.0
